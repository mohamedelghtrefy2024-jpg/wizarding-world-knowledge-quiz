# نقطة توقف آمنة — الجلسة الأولى: Wizarding World Knowledge Graph

## ما تم تنفيذه (مكتمل بالكامل، لا تُعاد مستقبلًا)

1. **بنية طبقات** مطابقة لمنهج مشروع مارفل: `index.html` (shell) + `css/base.css` + `css/components.css`
   + 8 ملفات JS في `js/` (config, utils, i18n, storageLayer, knowledgeLayer, businessLayer, graphLayer,
   renderLayer, app) + بيانات JSON منفصلة تمامًا في `data/`.
2. **UUID لكل عقدة** — لا اعتماد على `title` كمفتاح، فقط كخاصية عرض.
3. **68 عقدة، 69 علاقة، 8 حقب زمنية**، تغطي:
   - كل الأفلام/المسلسل/المسرحية/اللعبة (Hogwarts Legacy → Fantastic Beasts ×3 → Harry Potter ×8 →
     Cursed Child → مسلسل HBO) مع `storyOrder` وربط كل عمل بحقبته الزمنية.
   - المؤسسون الأربعة وبيوت هوجورتس.
   - شخصيات وعائلات حقبة Grindelwald (دمبلدور، جريندلوالد، نيوت، تينا وكوييني جولدستين، جاكوب،
     كريدنس، ليتا ليسترينج، باثيلدا باجشوت، ناجيني).
   - حدث 1945 (Battle of Godric's Hollow) وعصا الشيخوخة.
   - حقبة توم ريدل/فولدمورت وعائلة Gaunt ومفهوم الـHorcrux.
   - الثلاثي الرئيسي وعائلات Potter/Weasley/Malfoy/Black/Lestrange/Scamander/Goldstein، مع كل
     الروابط العائلية الموثقة (نيوت↔رولف↔لونا، Anthony Goldstein، Bellatrix↔Leta↔Draco↔Sirius).
   - منظمات (وزارة السحر، جيش الفينيق، أكلة الموت، جيش دمبلدور، MACUSA) وأماكن رئيسية.
   - الجيل الجديد (Albus Severus، Scorpius) من Cursed Child.
4. **كل رابط له مصدر (`source`) صريح** يحدد هل هو Canon مباشر من فيلم/كتاب، أو تصريح من J.K. Rowling،
   أو مواد ما بعد السلسلة — نفس فلسفة "توثيق حقيقي مش استنتاج" المتبعة في مارفل.
5. **i18n كامل عربي/إنجليزي** (كل نصوص الواجهة + تسميات الأنواع والعلاقات).
6. **4 واجهات عرض شغالة**: قوائم (rows) مقسّمة حسب الحقبة، شبكة D3 تفاعلية بألوان حسب النوع
   + legend، خط زمني كامل لكل الأعمال، ولوحة إحصائيات (توزيع الأنواع + الأكثر ارتباطًا).
7. **بحث وفلترة** حسب النوع، شغّالة في عرض القوائم والشبكة.
8. **صفحة فحص سلامة بيانات مستقلة** (`tests/data-integrity.html`).
9. **أمان أساسي**: كل نص ديناميكي بـ`textContent`، مفيش `innerHTML` في أي مكان.

## نتائج الاختبار الفعلي

- فحص `node --check` على كل ملفات JS: صفر أخطاء صياغة.
- كل ملفات JSON صالحة (`json.load` نجح على السبعة كلهم).
- **اختبار end-to-end حقيقي** عبر `vm` (كود المشروع الفعلي، مش محاكاة): تحميل البيانات الحقيقي،
  `validateIntegrity()` رجّعت **صفر مشاكل**، `computeMetrics()` رجّعت 68/69/8 بالظبط ومطابقة للعدّ اليدوي.
- التوزيع: 11 فيلم/مسلسل + مسرحية + لعبة (13 عمل)، 29 شخصية، 8 عائلات، 6 منظمات، 4 بيوت، 4 أماكن،
  2 غرض سحري، 1 حدث.

## ما لم يتم تنفيذه بعد (خارج نطاق الجلسة الأولى — لا تعتبره ناقصًا، ده تخطيط للمرحلة الجاية)

- تفاصيل زمنية دقيقة داخل كل فيلم (زي `storyOrder` الفرعي جوه نفس السنة).
- مخلوقات سحرية (Niffler, Basilisk...) وتعويذات كنوع عقدة مستقل.
- لوحة "أقصر مسار بين عقدتين" أو اكتشاف مجموعات/clusters (زي مارفل جلسة 3).
- تصدير/استيراد بيانات (JSON export).
- توسيع محتوى شخصيات ثانوية أكتر (Hogwarts Legacy، شخصيات المسلسل الجديد لما تتأكد رسميًا).
- اختبار Playwright حقيقي بمتصفح فعلي (اتعمل بس فحص منطق البيانات عبر Node `vm`، مش رندر DOM فعلي).

## الخطوة التالية المقترحة

نفس فلسفة مارفل: نوسّع نوع أو اتنين بعمق (مثلاً كل شخصيات Order of the Phoenix، أو كل الـHorcruxes
السبعة بالتفصيل) بدل ما نفتح كل الأنواع الناقصة دفعة واحدة.

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة الأولى مكتملة بالكامل ومُختبرة.**

---

## الجلسة الثانية — تنفيذ الاقتراح (Horcruxes + Order of the Phoenix)

### ما تم تنفيذه

1. **الـ7 Horcruxes بالتفصيل الكامل**:
   - 5 عقد جديدة من نوع `artifact`: Tom Riddle's Diary، Marvolo Gaunt's Ring، Salazar Slytherin's
     Locket، Helga Hufflepuff's Cup، Rowena Ravenclaw's Diadem.
   - Nagini وHarry Potter (موجودين مسبقًا) رُبطا كـHorcruxes عبر علاقة `created` من فولدمورت،
     مع توضيح أن حالة هاري كانت غير مقصودة (نتيجة ثانوية للعنة القتل الفاشلة سنة 1981).
   - كل الـ7 مربوطين بعلاقة `connected_to` بعقدة المفهوم "Horcrux (مفهوم)" الموجودة أصلًا.
   - علاقات `defeated` صريحة توثّق مين دمّر كل Horcrux: هاري (اليوميات)، دمبلدور (الخاتم)، رون
     (القلادة)، هيرمايني (الكأس)، نيفيل (ناجيني)، والتاج عبر لعنة Fiendfyre في معركة هوجورتس.
   - عقدة `event` جديدة: **Battle of Hogwarts (1998)** — لم تكن موجودة رغم مركزيتها، مربوطة بالتاج
     وناجيني والفيلم الأخير Deathly Hallows Part 2.

2. **توسيع جماعة الفينيق (Order of the Phoenix)** — كانت مربوطة بعضوين بس (دمبلدور، هاري):
   - 8 شخصيات جديدة: Minerva McGonagall، Rubeus Hagrid، Alastor "Mad-Eye" Moody، Nymphadora Tonks،
     Kingsley Shacklebolt، Arthur Weasley، Molly Weasley، Neville Longbottom.
   - علاقات `member_of` جديدة لكل الأعضاء دول + Sirius Black وRemus Lupin وSeverus Snape وRon
     وHermione اللي كانوا موجودين كعقد لكن من غير رابط عضوية فعلي بالجماعة.
   - علاقات وظيفية بهوجورتس (McGonagall أستاذة، Hagrid حارس، Moody أستاذ دفاع مؤقت).
   - عائلة Weasley اتربطت بعمق أكتر: Arthur وMolly `married_to` بعض، و`parent_of` لكل من Ron وGinny.

3. **جيش دمبلدور (Dumbledore's Army)** — كان مربوط بهاري بس كمؤسس:
   - أُضيفت عضوية Ron، Hermione، Ginny، Luna، Neville (كلهم موجودين كعقد من قبل، كانوا ناقصين
     الرابط فقط).

### نتائج الاختبار الفعلي (نفس منهجية الجلسة الأولى)

- `node --check` على كل ملفات JS التسعة: صفر أخطاء.
- كل ملفات JSON (نودز/إيدجز/جروبس/ميتاداتا/سيتينجز/i18n عربي وإنجليزي): صالحة.
- اختبار `validateIntegrity()` الفعلي عبر `vm` بكود المشروع نفسه: **صفر مشاكل** (لا IDs مكررة، لا
  عناوين متضاربة، لا edges معلّقة، لا أنواع غير معروفة).
- الأرقام الجديدة: **82 عقدة، 119 علاقة، 8 حقب زمنية** (زيادة 14 عقدة و50 علاقة عن الجلسة الأولى).
- التوزيع الجديد حسب النوع: 37 شخصية، 8 عائلات، 7 أغراض سحرية، 6 منظمات، 4 بيوت، 4 أماكن،
  2 حدث، 13 عمل (أفلام/مسلسل/مسرحية/لعبة).
- `metadata.json` اتحدّث ليعكس الأرقام الجديدة (nodeCount/edgeCount/groupCount).
- ما تم إضافة أي نوع node أو edge جديد في `config.js` — كل الإضافات استخدمت الأنواع الموجودة أصلًا
  (`created`, `defeated`, `connected_to`, `member_of`, `married_to`, `parent_of`) فمفيش داعي لتعديل
  `i18n` أو `NODE_TYPE_VISUALS`.

### ما لسه برّه النطاق (زي ما كان متوقع)

- مخلوقات سحرية وتعويذات كنوع node مستقل.
- لوحة "أقصر مسار" أو اكتشاف clusters.
- تصدير/استيراد JSON.
- اختبار Playwright حقيقي بمتصفح فعلي (لسه فحص منطق البيانات فقط عبر Node `vm`).

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة الثانية مكتملة بالكامل ومُختبرة (82 عقدة / 119 علاقة).**

---

## الجلسة الثالثة — مخلوقات سحرية وتعويذات كنوع عقدة مستقل

### ما تم تنفيذه

1. **نوعان جديدان في `config.js`**: `creature` و`spell`، بلون ونصف قطر خاص بكل منهم في
   `NODE_TYPE_VISUALS` (أخضر غابي للمخلوقات، بنفسجي للتعويذات)، وأُضيفا لمصفوفة `NODE_TYPES`.
2. **i18n عربي/إنجليزي**: تسميات `type.creature` و`type.spell` في الملفين.
3. **7 مخلوقات سحرية**: Basilisk، Buckbeak، Fawkes، Aragog، Norberta (تنين هاجريد)، Niffler، Thestral
   — كل واحد مربوط بمالكه/مرافقه (`owns`)، وبأول فيلم ظهر فيه (`first_appeared`)، وبعلاقات سردية
   مباشرة (مثلاً هاري `defeated` الباسيليسك، Fawkes `connected_to` الباسيليسك لإنقاذ هاري).
4. **7 تعويذات**: اللعنات الثلاث غير المغتفرة (Avada Kedavra، Imperius، Cruciatus) بخاصية
   `attributes.category`، بالإضافة لأربع تعويذات عملية شهيرة (Expelliarmus، Expecto Patronum،
   Wingardium Leviosa، Alohomora) — كل تعويذة مربوطة بالشخصية المرتبطة بها سرديًا وبأول فيلم ظهرت فيه.
5. **إضافتان جانبيتان لدعم الاتساق**: عقدة مكان جديدة **Forbidden Forest** (كانت غائبة رغم أهميتها
   لـAragog والقناطير)، وعقدة شخصية **Dobby** (الجني المنزلي، محوري في تحرره على يد هاري وتضحيته
   بحياته في Malfoy Manor) مع تصحيح اتجاه علاقة موته (بيلاتريكس هي من طعنته، وليس العكس).
6. **`metadata.json`** محدّث بالأرقام الجديدة وبقائمة `supportedNodeTypes` الكاملة.

### نتائج الاختبار الفعلي

- `node --check` على التسعة ملفات JS: صفر أخطاء (شمل `config.js` بعد التعديل).
- كل ملفات JSON صالحة syntactically.
- `validateIntegrity()` عبر `vm`: **صفر مشاكل** رغم إضافة نوعين جديدين بالكامل.
- الأرقام الجديدة: **98 عقدة، 154 علاقة، 8 حقب زمنية**.
- التوزيع: 38 شخصية، 8 عائلات، 7 أغراض سحرية، 7 مخلوقات، 7 تعويذات، 6 منظمات، 5 أماكن، 4 بيوت،
  2 حدث، 13 عمل.
- تأكدت إن الرندر (`renderLayer.js`, `graphLayer.js`) بيقرأ الألوان والتسميات ديناميكيًا من
  `CONFIG.NODE_TYPE_VISUALS` وi18n بدون أي قوائم أنواع مكتوبة يدويًا في مكان تاني، فمفيش أي كود
  إضافي مطلوب غير ملف الإعدادات نفسه.

### ما لسه برّه النطاق

- لوحة "أقصر مسار" بين عقدتين واكتشاف clusters.
- تصدير/استيراد بيانات JSON.
- اختبار Playwright حقيقي بمتصفح فعلي (بدل فحص منطق البيانات فقط عبر Node `vm`).
- تفاصيل زمنية دقيقة داخل كل فيلم (`storyOrder` فرعي).

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة الثالثة مكتملة بالكامل ومُختبرة (98 عقدة / 154 علاقة / 13 نوع عقدة).**

---

## الجلسة الرابعة — ميزة "أقصر مسار" واكتشاف المجموعات (Clusters)

هنا أول جلسة من غير أي تعديل على البيانات (nodes/edges) — كلها كود ميزات جديدة فوق نفس الـ98 عقدة.

### ما تم تنفيذه

1. **`KnowledgeLayer.shortestPath(fromId, toId)`** — خوارزمية BFS على تمثيل غير موجّه (undirected)
   للعلاقات (كل edge بيتعامل معه في الاتجاهين، لأن "إزاي العقدتين دول مرتبطين" مش مهتم مين بيشاور
   على مين). بترجع سلسلة العقد كاملة + العلاقات بينها، أو `null` لو مفيش اتصال، مع حماية من الـIDs
   الغلط (undefined) من غير ما تكسر.
2. **`KnowledgeLayer.detectClusters()`** — اكتشاف المكوّنات المتصلة (connected components) عبر DFS،
   مرتبة من الأكبر للأصغر، والعقد المعزولة بتظهر كمجموعة من عقدة واحدة.
3. **واجهة جديدة "🔗 المسار والمجموعات"**: تبويب في التولبار + قسم `view-path` في `index.html`.
   - قسم اختيار عقدتين (Select) وزرار "دوّر على المسار"، والنتيجة بتتعرض كسلسلة أزرار قابلة للنقر
     (بتفتح تفاصيل العقدة) مفصولة بعلاقاتها.
   - قسم المجموعات: بطاقة لكل cluster فيها العدد وكل الأعضاء كـchips قابلة للنقر.
4. **i18n كامل عربي/إنجليزي** لكل النصوص الجديدة (`path.*`, `clusters.*`, `toolbar.path`).
5. **CSS** متسق مع باقي المشروع (نفس ألوان `--gold`/`--burgundy`/`--panel`) في `components.css`.
6. الكود اتحط في `knowledgeLayer.js` (منطق خالص، بدون DOM) و`renderLayer.js` (العرض فقط) — نفس فصل
   الطبقات المعمول بيه في باقي المشروع، مفيش أي منطق graph جوه `app.js` أو `businessLayer.js`.

### نتائج الاختبار الفعلي

- `node --check` على كل ملفات JS: صفر أخطاء.
- كل ملفات JSON صالحة.
- `validateIntegrity()`: صفر مشاكل (البيانات نفسها متغيّرتش).
- **اختبار حقيقي لخوارزميات المسار والـclusters عبر `vm`** بكود المشروع الفعلي:
  - `Harry Potter → Gellert Grindelwald` عبر Order of the Phoenix → Dumbledore (3 خطوات).
  - `Newt Scamander → Tom Riddle/Voldemort` عبر Dumbledore (خطوتين).
  - `Dobby → Rowena Ravenclaw` عبر هاري → الأوردر → ماكجونجل → هوجورتس (5 خطوات).
  - عقدة لنفسها (`Harry → Harry`) بترجع صفر خطوات صح.
  - ID غلط/غير موجود بيرجع `null` من غير ما يكسر (اختبرته عن قصد بعنوان خطأ).
  - **اكتشاف حقيقي مفيد**: `detectClusters()` كشف إن فيه **9 عقد معزولة تمامًا** مالهاش أي علاقة
    واحدة في الجراف كله: Hogwarts Legacy، Fantastic Beasts 2، Fantastic Beasts 3، Half-Blood Prince،
    Deathly Hallows Part 1، مسلسل HBO، Ministry of Magic، Hogsmeade، Diagon Alley. ده مش باج في
    الميزة — ده فجوة حقيقية في التوثيق (مفيش أي `appears_in`/`connected_to` مربوطها بالشخصيات أو
    بالأفلام التانية)، وده مرشّح واضح لجلسة مستقبلية.

### ما لسه برّه النطاق

- تصدير/استيراد بيانات JSON.
- اختبار Playwright حقيقي بمتصفح فعلي (لسه فحص منطق الكود فقط عبر Node `vm`، مش رندر DOM حقيقي في
  متصفح).
- سد الفجوة اللي اكتشفناها دلوقتي: ربط الـ9 عقد المعزولة بعلاقات فعلية.

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة الرابعة مكتملة بالكامل ومُختبرة (98 عقدة / 154 علاقة، + ميزة أقصر مسار وclusters).**

---

## الجلسة الخامسة — سد فجوة العزلة: ربط الـ9 عقد المعزولة (بيانات فقط، بدون كود جديد)

نفس فلسفة الجلسة الرابعة بالعكس: هنا تعديل بيانات بحت، صفر تعديل على أي ملف JS.

### ما تم تنفيذه

استُخدمت أنواع العلاقات الموجودة أصلًا فقط (`appears_in`, `first_appeared`, `connected_to`, `member_of`) —
مفيش أي تعديل على `config.js` أو `i18n`. كل رابط جديد له `source` صريح بنفس أسلوب الجلسات السابقة.

1. **Fantastic Beasts: The Crimes of Grindelwald** — `appears_in`/`first_appeared` لنيوت، تينا، كوييني،
   جاكوب، كريدنس (+ `first_appeared` له في الفيلم الأول اللي كان ناقص أصلًا)، ليتا (أول ظهور فعلي على
   الشاشة)، جريندلوالد (+ `first_appeared` في الفيلم الأول متخفيًا باسم Graves)، دمبلدور (أول ظهور له في
   السلسلة كلها)، ناجيني (أول ظهور بهيئة إنسانة/Maledictus).
2. **Fantastic Beasts: The Secrets of Dumbledore** — `appears_in` لنيوت، دمبلدور، جريندلوالد، جاكوب،
   كوييني، كريدنس (مع توضيح كشف هويته المزعومة كـAurelius Dumbledore وتضحيته بنفسه).
3. **Half-Blood Prince** — `appears_in` لهاري، رون، هرمايني، دمبلدور، دراكو، سناب، جيني، لونا، بيلاتريكس.
   + رابط جديد مهم كان ناقصًا فعليًا: **`defeated` سناب → دمبلدور** (لعنة Avada Kedavra في برج الفلك،
   تنفيذًا لطلب دمبلدور نفسه).
4. **Deathly Hallows – Part 1** — `appears_in` لهاري، رون، هرمايني، دوبي (آخر ظهور له، يُقتل فيه)،
   بيلاتريكس، دراكو، فولدمورت، ناجيني.
5. **Harry Potter (HBO series)** — `appears_in` لهاري، رون، هرمايني، دمبلدور، ماكجونجل، سناب، هاجريد،
   بالاستناد لتأكيدات HBO/Max الرسمية للتمثيل (Dominic McLaughlin، Arabella Stanton، Alastair Stout،
   John Lithgow، Janet McTeer، Paapa Essiedu، Nick Frost) — الموسم الأول يعيد سرد Philosopher's Stone.
   (تم التحقق من هذه المعلومة عبر بحث ويب لأنها بعد تاريخ آخر تحديث للمعرفة الداخلية.)
6. **Hogwarts Legacy** — `connected_to` لهوجورتس (المدرسة)، هوجسميد، والغابة المحرمة — الثلاثة مناطق
   قابلة للاستكشاف فعليًا داخل اللعبة (مصدر: مواد اللعبة الرسمية المرخّصة من WB).
7. **Ministry of Magic** — `member_of` لآرثر ويزلي (قسم إساءة استخدام أدوات الُمَغَل) وكينجسلي شاكلبولت
   (عون سحري، ووزير لاحقًا)، + `connected_to` لفيلمي Order of the Phoenix (المحاكمة/معركة قاعة النبوءات)
   وDeathly Hallows Part 1 (التسلل لاسترجاع القلادة).
8. **Diagon Alley** — `connected_to` للفيلم الأول (أول زيارة لشراء المستلزمات ولقاء أوليفاندر) وHalf-Blood
   Prince (اقتحام بورجن آند بيركس ومتجر Weasleys' Wizard Wheezes).
9. **Hogsmeade** — `connected_to` لـPrisoner of Azkaban (أول زيارة + الكوخ الصارخ) وOrder of the Phoenix
   (اجتماع Hog's Head لتأسيس جيش دمبلدور)، بالإضافة لرابط Hogwarts Legacy أعلاه.

**إجمالي 53 رابط جديد** (بعضها ناتج كتصحيح فجوات توثيق فعلية اكتُشفت أثناء العمل، مش بس لسد العزلة —
مثل `first_appeared` كريدنس/جريندلوالد للفيلم الأول اللي كانا ناقصين تمامًا من قبل، ورابط سناب/دمبلدور
اللي مفيش أهم منه في كل السلسلة وكان مفقود).

### نتائج الاختبار الفعلي

- `node --check` على كل ملفات JS التسعة: صفر أخطاء (متوقع، مفيش تعديل عليهم أصلًا).
- كل ملفات JSON صالحة syntactically (nodes/edges/groups/metadata/settings/i18n عربي وإنجليزي).
- **`validateIntegrity()` الفعلي عبر `vm`**: **صفر مشاكل** (لا IDs مكررة، لا عناوين متضاربة، لا edges
  معلّقة، لا أنواع غير معروفة).
- **`computeMetrics()`**: 98 عقدة (لم تتغير)، **207 علاقة** (154 + 53)، 8 حقب زمنية.
- **`detectClusters()` — النتيجة الحاسمة**: **cluster واحد فقط يضم كل الـ98 عقدة، صفر عقد معزولة**
  (مقابل 9 عقد معزولة في نهاية الجلسة الرابعة). الفجوة المكتشفة سابقًا اتقفلت بالكامل.
- `metadata.json` محدّث (`edgeCount`: 207)، وتأكدت إن مفيش أي رقم قديم (154 أو غيره) متبقي مكتوب يدويًا
  في أي ملف JS أو HTML.

### ما لسه برّه النطاق

- تصدير/استيراد بيانات JSON.
- اختبار Playwright حقيقي بمتصفح فعلي (لسه فحص منطق البيانات فقط عبر Node `vm`).
- تفاصيل زمنية دقيقة داخل كل فيلم (`storyOrder` فرعي).
- ربط Tina/Queenie/Jacob بالفيلم الأول (Fantastic Beasts and Where to Find Them) بروابط `appears_in`
  صريحة — مش عزلة (متصلين فعليًا بعلاقات عائلية/تحالف)، لكن الفيلم نفسه لسه ناقصه توثيق مين ظهر فيه
  تحديدًا؛ مرشّح جيد لجلسة توثيق مستقبلية لو حبينا دقة أعلى.

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة الخامسة مكتملة بالكامل ومُختبرة (98 عقدة / 207 علاقة / صفر عقد معزولة).**

---

## الجلسة السادسة — تصدير/استيراد بيانات JSON

كود ميزة جديدة فوق نفس الـ98 عقدة / 207 علاقة — صفر تعديل على البيانات نفسها.

### ما تم تنفيذه

1. **`KnowledgeLayer.exportSnapshot()`** — يرجّع نسخة كاملة من الجلسة الحالية
   (`nodes`, `edges`, `groups`, `metadata`, + `exportedAt` و`schemaVersion`)، منطق خالص بدون أي DOM.
2. **`KnowledgeLayer.importSnapshot(snapshot)`** — يتحقق من الشكل الأساسي (`nodes[]`/`edges[]` لازم
   يكونوا arrays)، يستبدل البيانات الحالية مؤقتًا، يشغّل `validateIntegrity()` الحقيقي، ولو فيه أي مشكلة
   (IDs مكررة، edges معلّقة...) **يرجّع البيانات القديمة زي ما كانت بالظبط (rollback) ويرجع قائمة
   المشاكل** — الاستيراد الفاشل ميكسرش الجلسة الحالية أبدًا. البيانات دي في جلسة المتصفح بس (مفيش أي
   كتابة على ملفات `/data` نفسها، ده متسق مع طبيعة المشروع كـstatic files).
3. **أدوات في `utils.js`**: `Utils.downloadJSON(filename, obj)` (تنزيل ملف JSON عبر Blob) و
   `Utils.readFileAsJSON(file)` (قراءة ملف مرفوع وتحويله JSON، بيرفض برسالة عربية واضحة لو الملف تالف).
4. **`RenderLayer.showToast(message, kind)`** — رسالة صغيرة مؤقتة (نجاح/خطأ) لتغذية راجعة عن التصدير
   والاستيراد، بدون أي `innerHTML`.
5. **واجهة**: زرين جدد في التولبار "⬇️ تصدير" و"⬆️ استيراد" + `<input type="file">` مخفي، وربطهم في
   `app.js` (`_wireDataTransfer`). بعد استيراد ناجح: إعادة رسم الواجهة الحالية وتحديث تسميات i18n مباشرة.
6. **i18n كامل عربي/إنجليزي** لكل النصوص الجديدة (`toolbar.export`, `toolbar.import`, `data.exportSuccess`,
   `data.importSuccess`, `data.importError`).
7. **CSS** للزرين الجدد وللـtoast، متسق مع نفس ألوان المشروع (`--steel`, `--gold`, `--burgundy`,
   `--emerald`).

### نتائج الاختبار الفعلي

- `node --check` على كل ملفات JS: صفر أخطاء.
- كل ملفات JSON (شمل i18n المحدَّثين): صالحة.
- **اختبار حقيقي لدورة تصدير/استيراد كاملة عبر `vm`** بكود المشروع الفعلي:
  - تصدير: رجّع 98 عقدة / 207 علاقة + `exportedAt` صحيح.
  - **استيراد نفس النسخة المُصدَّرة (round-trip)**: صفر مشاكل تكامل، والأعداد اتطابقت بالظبط، و
    `metadata.edgeCount` اتحدّث لـ207.
  - **استيراد نسخة متعمّدًا فيها edge معلّق (dangling)**: `importSnapshot` رجّع المشكلة بالظبط
    (`"Edge bad-1 has dangling "from": does-not-exist"`) **ورجّع البيانات لحالتها الأصلية تمامًا**
    (تأكدت بالعدّ قبل/بعد).
  - **استيراد بيانات غير صالحة الشكل** (`{foo: "bar"}` بدون `nodes`/`edges`): رجّع رسالة خطأ عربية
    واضحة من غير ما يكسر أو يمسح البيانات الحالية.
  - بعد كل السيناريوهات: `validateIntegrity()` لسه صفر مشاكل و`detectClusters()` لسه cluster واحد
    يضم كل الـ98 عقدة — يعني الميزة الجديدة معملتش أي أثر جانبي على البيانات الأصلية.

### ما لسه برّه النطاق

- اختبار Playwright حقيقي بمتصفح فعلي (لسه فحص منطق الكود فقط عبر Node `vm`، مش تفاعل DOM/ملفات حقيقي
  زي ضغط زرار فعلي أو اختيار ملف من `<input type="file">` في متصفح حقيقي).
- تفاصيل زمنية دقيقة داخل كل فيلم (`storyOrder` فرعي).
- توثيق أدق لظهور Tina/Queenie/Jacob تحديدًا في الفيلم الأول (Fantastic Beasts and Where to Find Them).

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة السادسة مكتملة بالكامل ومُختبرة (98 عقدة / 207 علاقة + ميزة تصدير/استيراد JSON مع rollback عند فشل التكامل).**

---

## الجلسة السابعة — إغلاق فجوة توثيق Fantastic Beasts 1 + محاولة اختبار Playwright حقيقي

### ما تم تنفيذه

1. **سد الفجوة الموثّقة من الجلسة الخامسة**: تينا وكوييني جولدستين وجاكوب كوالسكي كانوا متصلين بالجراف
   (عبر علاقات عائلية/تحالف) لكن من غير أي رابط `first_appeared`/`appears_in` لأول فيلم ظهروا فيه فعليًا
   (Fantastic Beasts and Where to Find Them، 2016). أُضيف `first_appeared` للثلاثة تجاه الفيلم الأول —
   ده أول ظهور فعلي لهم على الشاشة، فمش `appears_in` عادي.
2. **محاولة جادة لتشغيل اختبار Playwright حقيقي بمتصفح فعلي** (بدل فحص منطق البيانات فقط عبر `vm`):
   - `npx playwright --version` نجح (الحزمة مثبّتة: 1.56.0).
   - `npx playwright install chromium` و`--with-deps` **فشلوا فعليًا** بسبب قيود شبكة صريحة في بيئة
     التنفيذ (الوصول لمصادر تنزيل متصفح Chromium ولمستودعات apt الخاصة بالـdependencies مش ضمن قائمة
     النطاقات المسموحة لهذه الجلسة). ده قيد بيئة تنفيذ، مش قرار تجاهل نطاق.
   - **البديل الفعلي المتاح والمُشغّل بنجاح**: صفحة `tests/data-integrity.html` الموجودة أصلًا شغالة
     ديناميكيًا بالكامل (بتعمل `fetch()` حقيقي على ملفات `/data` وتستخدم `KnowledgeLayer` الحقيقي، صفر
     أرقام مكتوبة يدويًا) — دي أقرب حاجة لاختبار متصفح حقيقي متاحة في السياق الحالي، وتحتاج تشغيلها من
     خلال متصفح فعلي (أو خادم محلي) لإثبات نجاحها البصري، وهو خارج قدرة بيئة التنفيذ دي.

### نتائج الاختبار الفعلي

- `node --check` على كل ملفات JS: صفر أخطاء.
- كل ملفات JSON صالحة.
- `validateIntegrity()` عبر `vm`: **صفر مشاكل**.
- `computeMetrics()`: 98 عقدة، **210 علاقة** (207 + 3)، `first_appeared` بقت 24 (كانت 21).
- `detectClusters()`: لسه cluster واحد يضم كل الـ98 عقدة.
- `metadata.json` محدّث (`edgeCount`: 210).

### ما لسه برّه النطاق

- **اختبار Playwright حقيقي بمتصفح فعلي**: يحتاج تشغيله في بيئة عندها إنترنت كامل (خارج هذا الـsandbox)
  لتنزيل متصفح Chromium. الخطوات جاهزة (`npx playwright install chromium` ثم كتابة سيناريو يفتح
  `index.html` عبر خادم محلي ويتأكد من ظهور العقد في كل الواجهات الأربعة) — لكن التنفيذ الفعلي محتاج بيئة
  تانية.
- تفاصيل زمنية دقيقة داخل كل فيلم (`storyOrder` فرعي).

---
**حالة المشروع: ✅ نقطة توقف آمنة — الجلسة السابعة مكتملة (98 عقدة / 210 علاقة، صفر عقد معزولة، صفر فجوات توثيق معروفة). اختبار Playwright الحقيقي بمتصفح لسه محتاج بيئة بإنترنت كامل.**
